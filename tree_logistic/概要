线性回归缺点：当数据拥有多特征且特征之间的关系复杂时候，构建模型难且笨拙
决策树ID3：这是一种贪心算法，它要在给定的时间内做出最佳选择，但不关心是否做到了全局最优。一旦将某特征切分欧，
          该特征将不再起作用，切割过于基金，不能直接出路连续型特征，只能事先变成离散型，这破坏了连续型
          变量的内在性质
CART：分类树回归，使用二元切分来处理连续型变量，叶节点的数据类型不是离散型而是连续型